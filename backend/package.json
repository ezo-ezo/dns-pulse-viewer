{
  "name": "dns-analyzer-backend",
  "version": "1.0.0",
  "description": "Live DNS packet capture backend for DNS Analyzer",
  "type": "module",
  "main": "backend-server.js",
  "scripts": {
    "start": "node backend-server.js"
  },
  "keywords": ["dns", "packet-capture", "websocket"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cap": "^0.2.1",
    "dns-packet": "^5.6.1",
    "ws": "^8.18.0"
  }
}
